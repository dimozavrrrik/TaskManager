# Инструкция по запуску TaskManager (Production)

## ✅ Все готово к запуску!

Ваше приложение полностью настроено и готово к использованию.

### 1. Запуск всех сервисов

```bash
# Запустите все сервисы через Docker Compose
docker-compose up -d

# Проверьте статус
docker ps
```

### 2. Доступ к приложению

После успешного запуска:

- **Frontend**: http://localhost:8081
- **Backend API**: http://localhost:8080
- **PostgreSQL**: localhost:5432

### 3. Первый запуск

1. Откройте браузер: http://localhost:8081
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Имя: ваше имя
   - Email: ваш email
   - Отдел: например, "Разработка"
   - Должность: например, "Разработчик"
   - Пароль: минимум 8 символов
4. После регистрации вы автоматически войдете в систему
5. Создайте свою первую задачу!

### 4. Тестирование API напрямую

```bash
# Проверка здоровья API
curl http://localhost:8080/api/v1/health

# Регистрация через API
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Иван Иванов",
    "department": "Разработка",
    "position": "Разработчик",
    "email": "ivan@example.com",
    "password": "SecurePass123!"
  }'

# Вход через API
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "ivan@example.com",
    "password": "SecurePass123!"
  }'
```

### 5. Полезные команды

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f frontend
docker-compose logs -f api
docker-compose logs -f postgres

# Перезапуск сервисов
docker-compose restart

# Остановка всех сервисов
docker-compose down

# Остановка с удалением данных БД
docker-compose down -v

# Пересборка и запуск
docker-compose up -d --build
```

### 6. Архитектура

```
┌─────────────┐
│   Browser   │
│ :8081       │
└──────┬──────┘
       │
       │ HTTP
       ▼
┌─────────────┐
│   Nginx     │  ← Статика Blazor WASM
│  (Frontend) │  ← Proxy /api → Backend
└──────┬──────┘
       │
       │ /api/* → :8080
       ▼
┌─────────────┐
│   Go API    │
│ :8080       │
└──────┬──────┘
       │
       │ PostgreSQL
       ▼
┌─────────────┐
│  PostgreSQL │
│ :5432       │
└─────────────┘
```

### 7. Возможности приложения

✅ **Аутентификация**
- Регистрация с email/password
- JWT токены (Access 15 мин + Refresh 7 дней)
- Безопасное хеширование паролей (bcrypt)

✅ **Dashboard**
- Статистика по задачам
- Последние задачи
- Быстрый доступ

✅ **Задачи**
- Создание задач
- Фильтрация и поиск
- Изменение статуса
- Управление участниками
- Архивирование

✅ **Сотрудники**
- Список всех сотрудников
- Поиск по разным полям
- Просмотр задач сотрудника

✅ **UI/UX**
- Material Design
- Адаптивный дизайн
- PWA поддержка

### 8. Production настройки

⚠️ **ВАЖНО для production:**

1. **Измените JWT_SECRET** в `.env`:
   ```
   JWT_SECRET=your-random-32+characters-secret-key-here
   ```

2. **Настройте HTTPS** через reverse proxy (nginx/traefik/caddy)

3. **Настройте резервное копирование БД**

4. **Настройте мониторинг и логирование**

5. **Обновите CORS** в API для разрешенных доменов

### 9. Устранение проблем

**Frontend не загружается:**
```bash
docker logs taskmanager_frontend
curl http://localhost:8081
```

**API не отвечает:**
```bash
docker logs taskmanager_api
curl http://localhost:8080/api/v1/health
```

**БД не подключается:**
```bash
docker logs taskmanager_postgres
docker exec -it taskmanager_postgres psql -U taskmanager -d taskmanager
```

**Сброс всего:**
```bash
docker-compose down -v
docker-compose up -d
```

### 10. Следующие шаги

1. Ознакомьтесь с полной документацией в [README.md](README.md)
2. Настройте production deployment
3. Настройте CI/CD pipeline
4. Добавьте дополнительные функции

---

🎉 **Приложение готово к использованию!**

Откройте http://localhost:8081 и начните работать с TaskManager!
